using UnityEngine;

// 임시 비활성화: Player Settings가 새 Input System이라 구 Input API 사용 불가
// Active Input Handling을 "Both"로 변경 후 주석 해제
public class CameraController : MonoBehaviour
{
    // 빈 클래스 — Input System 충돌 방지
}
